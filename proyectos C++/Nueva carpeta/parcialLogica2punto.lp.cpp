nombre_estudiante(juan,materia_clases,nota_algebra,nota_programacion,nota_calculo,nota_quimica,nota_fisica,nota_sociales,nota_catedra).
nombre_estudiante(david,materia_clases,nota_algebra,nota_programacion,nota_calculo,nota_quimica,nota_fisica,nota_sociales,nota_catedra).
nombre_estudiante(alejandra,materia_clases,nota_algebra,nota_programacion,nota_calculo,nota_quimica,nota_fisica,nota_sociales,nota_catedra).
nombre_estudiante(karen,materia_clases,nota_algebra,nota_programacion,nota_calculo,nota_quimica,nota_fisica,nota_sociales,nota_catedra).
nombre_estudiante(oscar,materia_clases,nota_algebra,nota_programacion,nota_calculo,nota_quimica,nota_fisica,nota_sociales,nota_catedra).
nombre_estudiante(laura,materia_clases,nota_algebra,nota_programacion,nota_calculo,nota_quimica,nota_fisica,nota_sociales,nota_catedra).
nombre_estudiante(patricia,materia_clases,nota_algebra,nota_programacion,nota_calculo,nota_quimica,nota_fisica,nota_sociales,nota_catedra).
nombre_estudiante(diana,materia_clases,nota_algebra,nota_programacion,nota_calculo,nota_quimica,nota_fisica,nota_sociales,nota_catedra).
nombre_estudiante(enrique,materia_clases,nota_algebra,nota_programacion,nota_calculo,nota_quimica,nota_fisica,nota_sociales,nota_catedra).
nombre_estudiante(valentina,materia_clases,nota_algebra,nota_programacion,nota_calculo,nota_quimica,nota_fisica,nota_sociales,nota_catedra).

materia_clases(albebra,programacion,calculo,quimica,fisica,sociales,catedra).

nota_algebra(3,8) :- aprobada.
nota_programacion(2,8):-perdida.
nota_calculo(3,2):-aprobada.
nota_quimica(3,5):-aprobada.
nota_fisica(4,2):_aprobada.
nota_sociales(4,7):-aprobada.
nota_catedra(3,2):-aprobada.



